# 🤖 Browser AI Agent

Автономный AI-агент для автоматизации работы в браузере. Агент может выполнять сложные задачи: искать информацию, заполнять формы, делать покупки и многое другое.

## ✨ Возможности

- 🌐 Автоматическая навигация по веб-сайтам
- 🔍 Поиск и анализ контента на страницах
- 🖱️ Взаимодействие с элементами (клики, ввод текста)
- 🔐 Автоматическое ожидание входа в аккаунты
- 🛡️ Обнаружение и обработка капчи
- 🔒 Система безопасности (guardrails) для защиты от нежелательного использования
- 🎯 Умное завершение задач без лишних итераций

## 📋 Требования

- Python 3.8 или выше
- Playwright (установится автоматически)
- API ключ OpenRouter

## 🚀 Быстрый старт

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/ANDREWms2089/AIHelper.git
```

### 2. Перейдите в папку проекта

```bash
cd AIHelper
```

Или если вы сохранили проект в другом месте, перейдите туда:

```bash
cd /путь/к/вашей/папке/AIHelper
```

### 2. Установите зависимости

```bash
pip3 install -r requirements.txt
```

### 3. Установите браузеры для Playwright

```bash
playwright install chromium
```

### 4. Настройте переменные окружения

Создайте файл `.env` в корне проекта:

```bash
cp .env.example .env
```

Откройте `.env` и добавьте ваш API ключ OpenRouter:

```
OPENROUTER_API_KEY=your_api_key_here
OPENROUTER_MODEL=openai/gpt-4o-mini
```

### 5. Запустите проект

```bash
python3 main.py
```

## 🎯 Как это работает

1. **Агент анализирует задачу** и определяет необходимые действия
2. **Открывает браузер** (видимый или headless)
3. **Навигация и взаимодействие** с элементами страницы
4. **Автоматическое ожидание** входа в аккаунты (если требуется)
5. **Обнаружение капчи** с возможностью ручного прохождения
6. **Умное завершение** при достижении результата

## ⚙️ Настройки

В файле `config.py` можно изменить:

- `BROWSER_HEADLESS` - режим браузера (True/False)
- `BROWSER_START_URL` - начальный URL
- `MAX_ITERATIONS` - максимальное количество итераций
- `OPENROUTER_MODEL` - модель AI для использования

## 🔒 Безопасность

Проект включает систему guardrails для защиты от:

- ❌ Нежелательных запросов (jailbreak, prompt injection)
- ❌ Раскрытия персональных данных (PII)
- ❌ Вредоносного контента
- ⚠️ Деструктивных действий (требуют подтверждения)

## 📝 Структура проекта

```
browser-ai-agent/
├── main.py                 # Точка входа
├── ai_agent.py             # Основная логика агента
├── ai_providers.py         # Провайдеры AI (OpenRouter)
├── browser_controller.py   # Управление браузером
├── page_analyzer.py        # Анализ страниц
├── context_manager.py      # Управление контекстом
├── element_finder.py       # Поиск элементов
├── security_layer.py       # Слой безопасности
├── guardrails.py           # Система ограждений
├── config.py               # Конфигурация
├── requirements.txt        # Зависимости
└── README.md              # Этот файл
```

## 🛠️ Разработка

Для разработки рекомендуется использовать виртуальное окружение:

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы, создайте issue в репозитории.

---

**Приятного использования! 🚀**

